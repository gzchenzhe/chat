# Visual baselines

`export-baseline-light.png` is generated from `tests/fixtures/export-baseline-state.json` with the application's normal 3x PNG export flow.

- Expected dimensions: `1125 × 2436` (`375 × 812` at 3x).
- Baseline SHA-256 at creation: `ec0053dec3605e73bcda8440571c8eb7aa00a6d5b13dab851cf39ddefdfb8bff`.

Regenerate it only after intentionally reviewing a visual change. Transient comparison output belongs in `test-results/`.
